// SIMPLE ADDITION IPO CALCULATOR BY JONATHAN

//Input
let number1 = +prompt("Enter a number:");
let number2 = +prompt("Enter another number");
let number3 = +prompt("Enter a last number:");
//Process
let total = (number1 + number2) * number3;

//Output
alert(`The result is:(${number1} + ${number2}) * ${number3} = ${total}`);
